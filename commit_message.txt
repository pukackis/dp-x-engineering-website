feat: Implement irregular header shape using CSS transforms

This commit introduces a dynamic, irregular shape to the header using CSS `transform: skewY()`. The header itself is skewed, and its content (navigation) is counter-skewed to remain straight, creating a visually appealing, non-flat transition.