fix: Ensure header text visibility in dark mode

This commit fixes the issue where header text (navigation links, language switcher, theme switcher) was invisible in dark mode due to conflicting color variables. Specific CSS rules have been added to ensure these elements are always visible with a light color in dark mode.