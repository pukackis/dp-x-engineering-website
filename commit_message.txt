fix: Add root index.html for GitHub Pages redirection

This commit adds a new `index.html` file to the root directory of the repository. This file serves as a redirect to the English version of the website (`en/index.html`), ensuring that GitHub Pages can correctly load and display the site.